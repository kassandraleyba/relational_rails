<body>
  <div class="specific-artist">
    <%= link_to "specific artist", "/artists/#{@artist.id}/works" %>
    <%= link_to "Welcome Page", "/" %>
  </div>

  <h1><%= @artist.name %></h1>
  <%# <div class="edit-button"> 
    <%= link_to "Edit #{artist.id}", "/artists/#{@artist.id}" %>
  <%# </div> %>
  <h3>From: <%= @artist.city %></h3>
  <h3>Alive Today: <%= @artist.alive_today %></h3>
  <p>Number of Works: <%= @artist.work_count %></p>
</body>